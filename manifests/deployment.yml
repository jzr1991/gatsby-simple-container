apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: gatsby-app
  name: gatsby-app
  namespace: finestorm
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gatsby-app
  template:
    metadata:
      labels:
        app: gatsby-app
    spec:
      containers:
      - image: quay.io/pne_roberts/gatsby-simple-container:ba23da4
        name: gatsby-simple-container
